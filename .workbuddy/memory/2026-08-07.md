# 2026-08-07 工作日志

## 项目扫描（datav-vue / DataS 可视化编辑器）
- 用户要求扫描项目以备后续使用，完成全项目结构探查。
- 画像已写入 MEMORY.md（技术栈、组件系统三件套、EditorModule、接口层、启动命令）。

## 启动 dev server
- 本机无 yarn，改用托管 node v22.22.2 直接运行：
  `NODE_ENV=development ./node_modules/.bin/vite`（等价 yarn dev）。
- 启动成功，监听端口 9090：http://localhost:9090/ （局域网 http://10.89.34.56:9090/）。
- 后台任务 id：D9oVRG（仍在运行）。
- 控制台有 public/ 下 json 应改用根路径引用的警告，非阻塞，可忽略。
