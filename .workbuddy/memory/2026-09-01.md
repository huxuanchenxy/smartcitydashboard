# 2026-09-01

- AgentConfig.vue 技能库改活：静态技能列表改为 onMounted 调 GET http://10.89.34.77:8080/api/skill/list（base 走 VITE_APP_DIFY_SESSION_HOST 覆盖，与 DifyRealDialog 直连约定一致）。字段映射 name/description/icon/skillId/isDeleted；接口失败或返回空时回退到静态兜底列表 FALLBACK_SKILLS，调试信息只走 console。注意 request 拦截器返回 axios 原始 response，取值用 resp.data.code / resp.data.data（不是 resp.code）。

- AgentConfig.vue 技能库 UI 调整（8 个技能要在一屏内放下、不出滚动条）：
  - 移除卡片右侧的「+」加号（.skill-add），整张卡片点击即 addSkill。
  - 保持单列布局，靠压尺寸换高度：right-panel padding 24→16、panel-header margin-bottom 20→10 / h3 20→18、skills-grid gap 12→10、卡片 padding 12→10、头像 40→32、标题 14→13 且 line-height 1.3、描述单行截断 line-height 1.3。8 张约 500px，笔记本视口也放得下。
  - 截断内容加悬停提示：skill-info 外包 el-tooltip（placement top-start / 深色 / show-after 300），#content 里渲染完整 name + desc（desc 为空不渲染），.skill-tip 限宽 240px 自动换行。Element Plus 已在 main.ts 全局注册（import from '@/components/element-plus'），无需单独 import；el-tooltip 不生成额外 wrapper，所以 skill-info 的 flex:1 + min-width:0 布局不受影响。
  - 用户偏好：技能库必须保持单列，不要改成两列/多列（曾改成两列被明确要求改回）。
