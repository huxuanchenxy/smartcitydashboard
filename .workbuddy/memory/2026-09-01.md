# 2026-09-01

- DifyRealDialog.vue：所有后端接口统一追加 loginAccount 查询参数（取 localStorage.getItem('loginAccount')，与登录页/nav-header 同 key）。
  - 新增 getLoginAccount() / withLoginAccount(url) 工具函数：自动判断 ? / & 拼接，未登录时不附加。
  - 覆盖 4 处：GET /api/session/list、GET /api/chat/sessionId（sessionId 仍走 axios params）、DELETE /api/session/delete/{sessionId}、WebSocket /ws/chat（含带 sessionId 与无参两种 URL）。

- DifyRealDialog.vue：新增 AI「挂起待确认」消息渲染（WebSocket 帧 status==='interrupted'）。
  - WS onmessage 新增 interrupted 分支：移除思考占位、push 一条 isInterrupted 助手消息，挂起后 isLoading=false 允许用户继续输入（回复「确认」即可执行）。
  - 字段健壮性：pending_question / pending_context 均先判空再使用（后端不一定每次下发）；pending_context 经 normalizePendingContext() 规整为条目数组——字符串按代码块、数组按表格（嵌套对象/数组用 pendingCellText 降级为 JSON）、其它按 JSON。
  - 模板：assistant 消息 message-content 改为 v-if 思考 / v-else-if 非中断 / v-else 中断面板（待确认徽标 + intent_code + 提问 + 上下文面板）。
  - 当前后端仅此一种 status；其他 status 后续由用户补充定义，暂不处理。

- DifyRealDialog.vue：新增 AI「任务完成」消息渲染（WebSocket 帧 status==='completed'）。
  - completed 分支：移除思考占位、push 一条 isCompleted 助手消息，展示 result 字段内容；pending_question / pending_context 为 null 时不渲染。
  - result 结构与 pending_context 一致，直接复用 normalizePendingContext() + pendingCellText() 渲染（「已完成」绿徽标 + intent_code + 「结果」上下文面板，SQL 走代码块、SQL_EXEC_AGENT.json 走表格）。
  - 模板消息链扩展为 v-if 思考 / v-else-if 非中断非完成 / v-else-if 中断 / v-else-if 完成。

- DifyRealDialog.vue：新增 WebSocket 帧兜底，杜绝「一直卡在思考中」。
  - onmessage 末尾兜底：已知分支（error/interrupted/completed/session_ready/content）均未命中且 isLoading 仍为真时，把原始帧 JSON.stringify 后原样渲染成一条助手消息（带「未在代码中预处理」前缀，含 status 提示），并结束思考态、清看门狗。
  - 帧解析失败（JSON.parse 抛错）路径此前已通过 endThinkingState 结束思考态，不会卡死；两者共同保证任何返回都不会无限停留「思考中」。
