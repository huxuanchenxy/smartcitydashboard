# 2026-09-02

- DifyRealDialog.vue：修复历史会话展示原始 JSON 帧的问题。
  - 原因：loadConversationMessages 把后端落库的 WS 原始 JSON 帧（status=interrupted/completed）直接当普通文本渲染。
  - 新增 parseAssistantHistoryContent(content)：仅对助手消息尝试解析——文本以 { 开头才 JSON.parse；status=interrupted → 待确认面板（pendingQuestion/pendingContext/intentCode），status=completed → 已完成面板（resultPayload），其它 status 或解析失败 → 原样文本展示（与实时链路兜底策略一致）。
  - 字段判空逻辑与 WS 实时分支完全一致（pending_question/pending_context/result 不一定下发）。
- DifyRealDialog.vue：精简重复解析逻辑——实时与历史统一走同一套帧解析。
  - 抽出共用 buildAssistantFrameFields(data)：status=interrupted/completed → 消息字段（isInterrupted/isCompleted/intentCode/pendingQuestion/pendingContext/resultPayload），其它 status 返回 null。
  - WS 的 interrupted / completed 两个重复分支合并为一个（共用判空、思考占位替换、收尾逻辑）；历史解析 parseAssistantHistoryContent 只做 JSON.parse + 调用共用映射。
  - TS 注意：messages.push 展开 Partial<ChartMessage> 时 content 变可选，需先给 content:'' 默认值再展开。
- DifyRealDialog.vue：附件上传从前端 mock 切换为真实接口（一个文件一个请求，选好即传）。
  - 接口：POST {UPLOAD_HOST}/api/file/upload/batch，FormData(loginAccount + files)，返回 data:[{accessUrl, filesId, originalFileName}]。
  - 新增 env VITE_APP_DIFY_UPLOAD_HOST（.env / .env.development / .env.production，均为 http://10.89.34.77:8080），未配置则回退 VITE_APP_DIFY_SESSION_HOST；已补 src/typings/env.d.ts 声明（顺带补了 USER_ID/SESSION_HOST/WS_HOST）。
  - 删掉 uploadFileMock，改为 uploadFile(File)；handleFileSelect 改 async 串行上传（先 Array.from(target.files) 再 target.value='' 再循环，保证同名文件可重复选）。
  - 失败不再静默移除条目：标记 error 保留在输入区（红色 upload-error 样式），发送时按 fileId 过滤忽略。
  - axios 0.21.1 传 FormData 时 xhr adapter 会自动 delete Content-Type（浏览器补 multipart boundary），无需手动设 header。
- DifyRealDialog.vue：无数据字段不再渲染空标题（"上下文信息"/"结果"）。
  - normalizePendingContext 增加 isEmptyContextValue 过滤（null/空串/空白串/空数组/{} 均视为无数据），归一化后只保留有内容的条目。
  - 归一化结果改为构建消息时算一次，存 ChartMessage.pendingContextEntries / resultEntries（PendingContextEntry 接口提到模块级）；模板按 length>0 判断，不再在模板里调方法（原先 v-if 与 v-for 各调一次）。
  - 模板兜底：entry.key 空不渲染 key 标题、text 空不渲染 pre、tableColumns 空不渲染 table。
  - pending_question 改为 trim 后判空，空白串不再渲染提问区。
  - isInterrupted/isCompleted 只在 buildAssistantFrameFields 里赋值（实时 WS 与历史解析共用），所以 entries 字段必然同步存在。
- DifyRealDialog.vue：WS 重连次数 3 → 10，并统一重连计数的重置。
  - 抽出 resetReconnect()（清 reconnectTimer + reconnectCount=0），调用点：socket.onopen（连接成功）、connectChat 切换会话分支、attemptReconnect 重连成功 then、closeWs（主动关闭）。
  - 原先只有 onopen 里 reconnectCount=0，切换会话/主动关闭都只清 timer 不清计数，残留次数会带到新会话（次数提到 10 后更明显）。
  - 新增 RECONNECT_DELAY=2000 常量（原为硬编码 2000），固定间隔 10 次约 20 秒耗尽；需要指数退避时改这一个常量即可。
  - wsStatus 目前只在逻辑里维护，UI 未展示（连接状态类调试信息按用户偏好走 console）。
- DifyRealDialog.vue：历史会话消息支持展示附件（后端 /api/chat/sessionId 新增 fileItemList 字段）。
  - 后端返回项：{ chatId, chatTime, content, type(0用户/1助手), sort, fileItemList:[{accessUrl, fileId, originalFileName}] }；注意没有 id 字段（原代码 a.sort ?? a.id 已改为 ?? 0 兜底），且不下发文件大小。
  - 新增接口 HistoryFileItem + normalizeFileItems(list?)（空数组/空项返回 undefined），在 loadConversationMessages 与 WS 实时回复帧、结构化帧（interrupted/completed）三处统一挂载到 message.files。
  - ChartMessage.files 结构扩展：size 改为可选（缺省不展示体积），新增 fileId / accessUrl。
  - 模板：附件块从「普通文本分支」内部移出到 .message-content 之后，与正文面板并列，三种消息类型都能显示附件；有 accessUrl 时文件名可点击打开 + ⬇ 下载；图片类（png/jpg/gif/webp/svg 等）显示 44px 缩略图，加载失败由 onThumbError+brokenThumbs 回退为扩展名图标（fileIcon）。
  - copyMessageContent 顺带复制附件名；「已完成」面板正文为空时回退复制 resultPayload JSON。
