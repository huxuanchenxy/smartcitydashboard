# 2026-09-03

- DifyRealDialog.vue：intent_code 不再展示给用户。
  - 移除"待确认"/"已完成"面板标题栏上的 intentCode 展示 span（interrupted-intent / result-intent），并清理对应无用 CSS。
  - ChartMessage.intentCode 字段与 data.intent_code 映射保留（内部数据，未再参与渲染）。
