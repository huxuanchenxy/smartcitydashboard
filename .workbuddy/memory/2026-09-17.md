# 2026-09-17

## DifyRealDialog.vue UI 重构（现代 AI 助手风格）
- 需求：按用户提供的参考截图重构 `src/components/dify-chatbot/DifyRealDialog.vue`（4281 行 → 4975 行）。
- **限制**：当前模型不支持读图，无法查看截图，重构依据“左侧会话栏 + 右侧欢迎页/消息卡片 + 底部输入区”的通用现代 AI 助手布局完成，未做像素级视觉校验。
- 做法：只替换 `<template>` 与 scoped `<style>`，完整保留 `<script>`（WS/会话/上传/flint 图表/技能交互/Markdown 编辑器/字体缩放）与全局 `<style>`（toast/confirm）。
- 新增能力：
  - props：`subtitle`、`suggestions`；`sidebarWidth` 默认 260
  - 状态：`sidebarTab`、`visibleConversations`(最近 6 条)、`showEarlierDivider`、`conversationTimeText`、`sidebarCollapsed`(<560px 自动收起)、`toggleSidebar`、`headerTitle`、`roleLabel`/`userInitial`/`showUserSub`、`queryInputRef`、`canSend`
  - 欢迎页：88px 品牌 logo + 标语 + 三张建议卡（`applySuggestion` 仅回填输入框并聚焦，不自动发送）
  - 消息气泡：用户右侧蓝气泡 / 助手左侧白卡片，`.message-footer` 时间 + 复制按钮同排
- 踩坑修复：
  1. `v-html` 注入的 SVG 无 scoped 属性 → 改用 `:deep(svg)` 控制尺寸
  2. 项目无全局 border-box reset，content-box 下 width:100%+padding 溢出 → 组件内 `.custom-dialog, .custom-dialog * { box-sizing: border-box; }`
  3. 复制按钮与时间分属两个 div 致布局错位 → 合并为 `.message-footer`
- 验证：`@vue/compiler-sfc` template/script/style 均编译通过；模板 `_ctx` 引用与 setup return 键交叉校验无真实缺失；`vue-tsc --noEmit` 两次均 0 错误；临时预览页 + 无头 Chrome 截图/尺寸诊断（已删除 `__uipreview`）。
- 仓库状态：`git status` 仅 ` M src/components/dify-chatbot/DifyRealDialog.vue`；备份在系统临时目录 `.../Temp/dify-ui-refactor/DifyRealDialog.vue.bak`。
- 新建技能：`~/.workbuddy/skills/vue-sfc-ui-verify`（大文件 SFC 分段替换 + 静态校验 + 预览诊断流程）。

## 后续微调：会话侧栏去掉分组
- 用户要求：不要「最近对话 / 历史记录」两个 tab，统一叫「历史记录」，也不要「更早」分组。
- 改动：删 `sidebarTab` / `RECENT_LIMIT` / `visibleConversations` / `showEarlierDivider` 及 `.sidebar-tabs/.sidebar-tab/.conversation-group-title` 样式；模板列表直连 `conversationList`，新增 `.sidebar-section-title` 静态标题「历史记录」，空态文案固定「暂无对话记录」。
- 校验：compiler-sfc 编译通过，模板引用 70 项全部在 setup return 中（缺的 7 项均为 props 误报）。

## 再微调：历史记录不显示时间
- 用户要求：对话历史列表不要显示日期时间。
- 改动：删模板 `.conversation-item-time` span、`conversationTimeText` 函数（含 setup return）、对应样式；`ConversationItem.updateTime` 字段与 `formatConversationTime` 映射保留（数据层仍存创建时间，暂不展示）。
- 校验：编译通过，无残留引用。
