# 2026-09-17

## DifyRealDialog.vue UI 重构（现代 AI 助手风格）
- 需求：按用户提供的参考截图重构 `src/components/dify-chatbot/DifyRealDialog.vue`（4281 行 → 约 4950 行）。
- **限制**：当前模型不支持读图，无法查看截图，重构依据"左侧会话栏 + 右侧欢迎页/消息卡片 + 底部输入区"的通用现代 AI 助手布局完成，未做像素级视觉校验。
- 做法：只替换 `<template>` 与 scoped `<style>`，完整保留 `<script>`（WS/会话/上传/flint 图表/技能交互/Markdown 编辑器/字体缩放）与全局 `<style>`（toast/confirm）。
- 新增能力：props `subtitle`、`suggestions`、`sidebarWidth` 默认 260；欢迎页引导卡；消息气泡 + `.message-footer`；窄窗 <560px 自动收起侧栏。
- 踩坑修复：`v-html` 注入 SVG 须用 `:deep(svg)`；组件内补 `box-sizing: border-box`（项目无全局 reset）；复制按钮与时间合并为一行。
- 备份：`C:\Users\Administrator\AppData\Local\Temp\dify-ui-refactor\DifyRealDialog.vue.bak`
- 新建技能：`~/.workbuddy/skills/vue-sfc-ui-verify`（大文件 SFC 分段替换 + 静态校验 + 预览诊断流程）。

## 微调一：会话侧栏去掉分组
- 用户要求：不要「最近对话 / 历史记录」两个 tab，统一叫「历史记录」，也不要「更早」分组。
- 改动：删 `sidebarTab` / `RECENT_LIMIT` / `visibleConversations` / `showEarlierDivider` 及相关样式；列表直连 `conversationList`，新增 `.sidebar-section-title` 静态标题。

## 微调二：历史记录不显示时间
- 用户要求：对话历史列表不要显示日期时间。
- 改动：删 `.conversation-item-time` span、`conversationTimeText`（含 return）、对应样式；`ConversationItem.updateTime` 与 `formatConversationTime` 保留在数据层暂不展示。

## 微调三：浮动弹窗改为视口 80%
- 用户反馈：从 `/#/admin/screen/346`（编辑器 header-toolbar）弹出的弹窗太小。
- 原因：浮动模式默认固定 600×600，默认位置 `x = innerWidth/2 + 50` 偏右未居中。
- 改动：新增 prop `viewportRatio`（默认 0.8）；`computeViewportSize/Position` 按视口算尺寸并居中；`syncSizeToViewport()` 在每次打开和 window resize 时重算，用户手动拖拽/缩放后（`userAdjusted`）不再干预；宿主传 `initialSize`/`initialPosition` 时以其为准（AgentConfig 走这条路）；inline 模式跳过。
- 顺带修复：欢迎页三张引导卡之前被整段 HTML 注释包住（未渲染），已放开。
- 校验：compiler-sfc 编译通过，模板引用 69 项全部有绑定，`vue-tsc --noEmit` 0 错误。

## 微调四：内容列加宽 20%
- 用户反馈：聊天区与输入区同宽但偏窄。
- 改动：新增 CSS 变量 `--chat-content-max: 940px`（在 `.custom-dialog` 变量块内），`.welcome-inner` / `.message-list` / `.input-wrapper` 三处原 `max-width: 780px` 改为 `max-width: var(--chat-content-max, 940px)`，约 +20%。以后调宽只改这一个变量。

## 微调五：用户气泡内附件文字看不清
- 现象：用户消息（蓝底气泡）里的附件名 / 大小 / 下载是白字，压在半透明白底卡片上几乎不可读。
- 改动：`.user-message .message-file-item` 改为实心白底 + `--chat-title` 深色文字；`.user-message .file-size/.file-download` 改 `--chat-text-sub`；`.user-message .file-name-link` 改 `--chat-primary-strong`（hover 用 `--chat-primary`）。助手侧白卡片附件不受影响。

## 组态鉴权头：上传接口 + WS 加 X-Src-System / token
- 需求：`/api/file/upload/batch` 与 `ws://…/ws/chat` 两个接口都要带 `X-Src-System: zutai01` 和 `token`。
- env：新增 `VITE_APP_DIFY_SRC_SYSTEM=zutai01`，写入 `viteEnv/.env`、`.env.development`、`.env.staging`、`.env.production`（staging 原本没有 DIFY 段，单加了这一项）。
- 组件内新增（`getLoginAccount` 下方）：`SRC_SYSTEM`、`getCurrentPath()`（hash 路由取 path）、`getUrlToken()`（URLSearchParams 自动 decode）、`getAuthToken()`——发布页 `#/publish/:id?token=xxx` 取 URL token，其余取 `localStorage['DataS-Token']`。
- 上传：`request.post(url, form, { headers: { 'X-Src-System', token } })`（axios 会自动补 multipart boundary）。
- WS：**浏览器原生 WebSocket 不能自定义请求头**，改为握手 URL 追加 query `X-Src-System=…&token=…`（再经 `withLoginAccount` 拼 loginAccount）。若后端只认 header，需改走子协议 `Sec-WebSocket-Protocol` 或换 HTTP 通道。
- 校验：compiler-sfc parse + compileScript 均通过。

## 问题排查：发布页与编辑器/预览页 UI 不一致
- 用户反馈：`#/publish/346` 的字体/按钮与 `#/admin/screen/346`、`#/screen/preview/346` 不一样。
- 已定位差异根因：
  - 发布页 `publish.vue` 走 `/screen/getSnapshot`（发布快照），读取的是发布时保存的快照数据。
  - 编辑器 `index-new.vue` 和预览页 `index.vue` 走 `/screen/getScreen`（实时大屏数据）。
  - 三个页面最终都用同一个 `screen.vue` 渲染组件，渲染逻辑完全一致；若视觉不同，只可能是快照数据与当前大屏数据不一致（最近修改后未重新发布）。
- 已抓取发布页无头截图，当前快照中 `VAiDifyReal` 的 `fontScale=1.7`、`sidebarWidth=300`、`zoomMode=responsive`、`attr=1920×1080`。
- 待用户确认：是否需要重新发布，还是改为保存时自动刷新快照。

## 清理排查临时文件
- 排查过程中生成了 `.tmp-cmp/`（Chrome 临时用户数据目录、API 响应、调试截图）和 `outputs/publish-346-screenshot.png`，已用 PowerShell 强制删除；清理后 `git status` 仅余 `.workbuddy/memory/2026-09-17.md` 的修改。
